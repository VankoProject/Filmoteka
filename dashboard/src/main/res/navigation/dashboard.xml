<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/dashboard"
    app:startDestination="@id/dashboardFragment">

    <fragment
        android:id="@+id/dashboardFragment"
        android:name="com.kliachenko.dashboard.presentation.DashboardFragment"
        android:label="Films"
        tools:layout="@layout/fragment_dashboard">

        <action
            android:id="@+id/action_dashBoardFragment_to_detailFragment"
            app:destination="@+id/detailFragment"
            app:enterAnim="@anim/nav_default_enter_anim"
            app:exitAnim="@anim/nav_default_exit_anim"
            app:popEnterAnim="@anim/nav_default_enter_anim"
            app:popExitAnim="@anim/nav_default_exit_anim"
            app:popUpTo="@id/dashboardFragment"/>
    </fragment>

    <fragment
        android:id="@+id/detailFragment"
        android:name="com.kliachenko.filmoteka.fragments.DetailFragment"
        android:label="Detail film info" >
        <argument
            android:name="filmId"
            app:argType="integer" />
    </fragment>

</navigation>